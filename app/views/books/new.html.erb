<h1>投稿フォーム</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :profile_image_id %>
  <h4>本のタイトル</h4>
  <%= f.text_field :title %>
  <h4></h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>